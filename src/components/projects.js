import React from 'react';

const info = {
    display: 'flex',
    flexDirection: 'column',
    padding: '10px'
}

const titleStyle = {
    backgroundColor: 'red',
    padding: '10px'
}

const linkStyle = {
    color: 'red'
}